import { EventsCalendar } from '../EventsCalendar';
import { ThemeProvider } from '../ThemeProvider';

export default function EventsCalendarExample() {
  return (
    <ThemeProvider>
      <EventsCalendar />
    </ThemeProvider>
  );
}